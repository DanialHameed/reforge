# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Auth / security
email-validator>=2.2.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==3.2.2  # VERIFY: pinned for compatibility; may be adjustable
cryptography>=42.0.8

# Async worker
celery>=5.4.0  # celery: async task queue
redis>=5.0.8  # redis: broker + result backend
kombu>=5.3.4

# Database
sqlalchemy>=2.0.30
alembic>=1.13.1
asyncpg>=0.29.0
aiosqlite>=0.20.0  # SQLite dev

# AI
google-generativeai>=0.7.0  # google-generativeai: Gemini SDK
google-genai>=1.0.0  # VERIFY: newer SDK used by AIService
google-api-python-client>=2.131.0
google-auth>=2.33.0

# HTTP client (for image fetching in GeminiService)
httpx>=0.27.0  # httpx: async HTTP for image fetching

# Media storage
cloudinary>=1.40.0

# Config/env
pydantic>=2.7.0
pydantic-settings>=2.3.0  # pydantic-settings: env var validation
python-dotenv>=1.0.1

# Monitoring (optional but strongly recommended)
flower>=2.0.1

# Utilities
cachetools>=5.3.0
tenacity>=8.3.0

# Media processing
pillow>=10.3.0
ffmpeg-python>=0.2.0
moviepy==1.0.3

# System metrics
psutil>=6.0.0

# Testing (dev-only)
pytest>=8.2.2  # VERIFY: dev-only
pytest-asyncio>=0.23.7  # VERIFY: dev-only

# VERIFY: may be unused (no imports found)
anthropic==0.29.0
aiofiles==23.2.1

